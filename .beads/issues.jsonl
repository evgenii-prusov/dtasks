{"_type":"issue","id":"dtask-jzw","title":"Execute First Deploy: point DNS at dtasks VM, docker compose up","description":"Phase of dtask-y23. The VM (dtask-h3h) is provisioned and verified: 92.5.41.82 in eu-frankfurt-1, Docker Engine running, ports 80/443 open at OCI security-list + OS firewall level. What's left is docs/deploy.md's 'First deploy' section: DNS A record -\u003e 92.5.41.82, git clone onto the VM, .env with real DOMAIN/ACME_EMAIL/DTASKS_INVITE_CODE, docker compose build \u0026\u0026 up -d, then the smoke checklist (signup over HTTPS, Secure+HttpOnly+SameSite cookie, anonymous /api/* -\u003e 401).","acceptance_criteria":"A friend can sign up with the invite code over HTTPS on the deployed instance; session cookie has Secure+HttpOnly flags; docker compose logs show alembic upgrade head + uvicorn startup.","status":"closed","priority":1,"issue_type":"task","owner":"prusove@gmail.com","created_at":"2026-07-15T20:17:51Z","created_by":"Evgenii Prusov","updated_at":"2026-07-18T12:46:19Z","closed_at":"2026-07-18T12:46:19Z","close_reason":"Verified live on dtasks.dev: HTTPS active, 1 real user account exists, session cookie has Secure+HttpOnly+SameSite=lax, unauthenticated /api/projects returns 401, alembic at head (confirmed via SSH during migration verification).","dependencies":[{"issue_id":"dtask-jzw","depends_on_id":"dtask-h3h","type":"blocks","created_at":"2026-07-15T22:17:52Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-h3h","title":"Provision Oracle Cloud VM via Terraform (reproducible IaC)","description":"Phase of dtask-y23: docs/deploy.md assumes the VM already exists, but VM creation itself (network, security rules, compute instance) was never scripted or documented. User confirmed Oracle account PAYG conversion completed 2026-07-14, unblocking actual VM provisioning. Deliverable: an OCI CLI bash script that creates the VCN/subnet/internet gateway/security list and launches an Ampere A1 Flex (2 OCPU/12GB, matches Always Free cap) instance with cloud-init installing Docker, plus a docs/provision-vm.md runbook covering one-time OCI CLI auth bootstrap and how to re-run/tear down.","acceptance_criteria":"Running the script (with a filled-in config) creates a working Ubuntu ARM64 VM reachable via SSH with Docker + compose plugin installed, ports 22/80/443 open at both the OCI security-list and OS firewall level, ready for docs/deploy.md's 'First deploy' section to run unmodified. docs/provision-vm.md lets someone reproduce or tear down the VM without prior OCI experience.","notes":"Applied successfully against the real eu-frankfurt-1 account (user ran 'terraform apply' themselves). All 7 resources created cleanly; verified via SSH: cloud-init status=done, Docker Engine 29.6.1 running, iptables ACCEPT rules for 80/443 present. Public IP: 92.5.41.82 (see terraform output in infra/ -- not committed, state/tfvars are gitignored). Ready for docs/deploy.md's 'First deploy' step once DNS is pointed at that IP. User is committing + opening the PR themselves.","status":"closed","priority":1,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-14T20:15:37Z","created_by":"Evgenii Prusov","updated_at":"2026-07-15T20:15:39Z","started_at":"2026-07-14T20:15:41Z","closed_at":"2026-07-15T20:15:39Z","close_reason":"Merged in PR #40. Verified: terraform apply ran clean against eu-frankfurt-1, VM reachable via SSH with cloud-init done, Docker Engine running, iptables 80/443 open.","dependencies":[{"issue_id":"dtask-h3h","depends_on_id":"dtask-y23.3","type":"blocks","created_at":"2026-07-14T22:15:40Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-y23.4","title":"Write docs/deploy.md: Docker-based ops runbook","description":"Tie together the password-reset script, migration-strategy, and Docker bundle tasks into a single ops runbook, so deploying/operating/recovering the app doesn't rely on tribal knowledge. Extends (doesn't duplicate) the README's existing one-paragraph '## Production' section.\n\n## Implementation -- cover:\n- Env var reference: DTASKS_INVITE_CODE, DTASKS_SECURE_COOKIES, DTASKS_SESSION_DIR, DTASKS_DB_PATH, and where they're set (compose env file).\n- Build \u0026 deploy steps: docker compose build \u0026\u0026 docker compose up -d.\n- Migration step: how/when 'alembic upgrade head' runs in the entrypoint.\n- Backup/restore instructions (cron + sqlite .backup against the volume).\n- How to run scripts/reset_password.py against the running container.\n- Invite-code handout process for beta friends.\n- One-line note for future-you: this VM may host additional unrelated apps later, each as a separate container + separate Caddy site block -- no action needed now.","acceptance_criteria":"A person with no prior context can follow docs/deploy.md alone to: deploy from scratch, recover a lost password, and restore from backup.","status":"closed","priority":1,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-13T20:40:49Z","created_by":"Evgenii Prusov","updated_at":"2026-07-13T21:57:28Z","started_at":"2026-07-13T21:53:34Z","closed_at":"2026-07-13T21:57:28Z","close_reason":"Wrote docs/deploy.md covering prerequisites, first deploy, env var reference, migrations, password reset, backup/restore, invite codes, and the multi-app-on-one-VM note. Every command in it was run and verified against the live stack: full backup-\u003edrift-\u003erestore drill (created a post-backup user, restored, confirmed it's gone and the pre-backup user survives) and reset_password.py run inside a live container (old password fails after reset, new one works). Also fixed two real gaps found while verifying: scripts/ wasn't copied into the image (reset_password.py couldn't run), and sqlite3 CLI wasn't installed in the slim python image (the documented .backup command would have failed) -- both added to the Dockerfile. README's Production section now links to the runbook.","dependencies":[{"issue_id":"dtask-y23.4","depends_on_id":"dtask-y23.1","type":"blocks","created_at":"2026-07-13T22:40:49Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-y23.4","depends_on_id":"dtask-y23.3","type":"blocks","created_at":"2026-07-13T22:40:49Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-y23.4","depends_on_id":"dtask-y23.2","type":"blocks","created_at":"2026-07-13T22:40:49Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-y23.4","depends_on_id":"dtask-y23","type":"parent-child","created_at":"2026-07-13T22:40:49Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-y23.3","title":"Dockerize dtasks + Caddy for single-VM deployment (Oracle Always Free/PAYG)","description":"Package the app for the chosen hosting target: a single Oracle Cloud 'Always Free' compute instance, upgraded to Pay-As-You-Go billing (removes idle-instance reclamation risk; usage stays $0 as long as it remains within Always Free resource caps -- 2 OCPU/12GB RAM, 200GB storage as of the June 2026 allocation). PAYG upgrade was requested and is pending as of 2026-07-13; note it triggers a one-time card verification hold (commonly reported around $93-100, refunded -- sometimes with a multi-day delay), which is separate from ongoing usage billing.\n\nDocker was chosen over bare systemd specifically because this VM may host additional unrelated hobby apps later, each isolated in its own container.\n\n## Implementation\n- Dockerfile: multi-stage build -- stage 1 builds the frontend (npm run build -\u003e frontend/dist), stage 2 is the Python runtime that serves it via the existing create_static_files_router setup in main.py; runs the migration-then-uvicorn entrypoint from the migration-strategy task.\n- docker-compose.yml: app service + Caddy service, sharing a network; named volume for the SQLite DB file + FileStore session directory so data survives container recreation/image updates.\n- Caddyfile: domain name -\u003e automatic HTTPS via Let's Encrypt -\u003e reverse_proxy to the app service.\n- Env var wiring for prod: DTASKS_INVITE_CODE, DTASKS_SECURE_COOKIES=1, DTASKS_SESSION_DIR, DTASKS_DB_PATH pointed at the mounted volume paths, passed via a compose env file (not committed to git).\n- Backup strategy: cron on the host running sqlite3 .backup against the volume-mounted DB path (or via docker compose exec) -- cron + sqlite .backup, not litestream (overkill for a friends beta).\n- Oracle/VM prerequisites (ops steps, not app code): confirm PAYG upgrade completed; install Docker + the docker compose plugin on the VM.\n\n## Verification\n- Manual smoke checklist: friend signs up over HTTPS with the invite code; Set-Cookie shows Secure + HttpOnly + SameSite=Lax; anonymous /api/* returns 401.\n- Restarting the containers (docker compose down \u0026\u0026 up) does not lose existing data (DB + sessions survive).","acceptance_criteria":"A friend can sign up with the invite code over HTTPS on the deployed instance; session cookie has Secure+HttpOnly flags in prod; container restarts preserve DB and session data; achieved via docker compose up on the Oracle VM.","status":"closed","priority":1,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-13T20:40:36Z","created_by":"Evgenii Prusov","updated_at":"2026-07-13T21:52:14Z","started_at":"2026-07-13T21:19:23Z","closed_at":"2026-07-13T21:52:14Z","close_reason":"Dockerfile (multi-stage: node frontend build + python/uv runtime), docker-compose.yml (app + Caddy, named volume for DB+sessions), Caddyfile (auto-HTTPS via {$DOMAIN}/{$ACME_EMAIL}), docker-entrypoint.sh (alembic upgrade head then uvicorn), .dockerignore, and .env.example added. Fixed a real bug found during review: DTASKS_DB_PATH was set to a subdirectory (/data/db/...) that SQLite can't auto-create; changed to /data/dtasks.sqlite directly under the volume root. End-to-end smoke test on this machine (after fixing a local broken Docker/OrbStack install) confirmed: anonymous /api/* returns 401; signup/login set-cookie has Secure+HttpOnly+SameSite=lax; alembic migrations run automatically at container startup; docker compose down \u0026\u0026 up (without -v) preserves the user account, confirming DB/session persistence via the named volume.","dependencies":[{"issue_id":"dtask-y23.3","depends_on_id":"dtask-y23.2","type":"blocks","created_at":"2026-07-13T22:40:36Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-y23.3","depends_on_id":"dtask-y23","type":"parent-child","created_at":"2026-07-13T22:40:36Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dtask-y23.2","title":"Decide migration strategy: alembic upgrade head vs create_all","description":"Alembic is already fully scaffolded (backend/migrations/, 3 revisions: 0001 baseline, 0002 multitenancy, 0003 green tasks) but backend/app/main.py's lifespan still unconditionally calls Base.metadata.create_all -- the two mechanisms currently coexist undocumented. This is a real risk for a fresh prod DB: it's unclear which one is authoritative, and a bug here silently produces schema drift relative to migration history.\n\n## Implementation\n- Make alembic authoritative for prod: remove/gate create_all() so it is not called at prod app startup (keep it for test fixtures only -- backend/tests/conftest.py).\n- Docker entrypoint (coordinate with the Docker bundle task): container startup runs 'alembic upgrade head' before starting uvicorn, so a fresh container always has an up-to-date schema with no manual step.\n- Document the decision inline only where non-obvious; the full narrative goes in docs/deploy.md.\n\n## Verification\n- Fresh empty DB + 'alembic upgrade head' produces the same schema as today's create_all().\n- Existing test suite stays green (tests keep using create_all() directly, unaffected by the prod code path change).","acceptance_criteria":"main.py no longer calls create_all() at prod startup; a fresh container running the entrypoint ends up with a fully migrated schema; test suite passes unchanged.","status":"closed","priority":1,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-13T20:40:15Z","created_by":"Evgenii Prusov","updated_at":"2026-07-13T21:19:16Z","started_at":"2026-07-13T20:42:27Z","closed_at":"2026-07-13T21:19:16Z","close_reason":"Implemented in dd2de26: main.py gates create_all behind DTASKS_AUTO_CREATE_SCHEMA (tests opt in via conftest.py), alembic is authoritative for prod. Verified alembic upgrade head on a fresh DB produces an equivalent schema to create_all, and full test suite (30 tests) passes.","dependencies":[{"issue_id":"dtask-y23.2","depends_on_id":"dtask-y23","type":"parent-child","created_at":"2026-07-13T22:40:14Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dtask-y23.1","title":"Password reset script for beta users","description":"Manual account-recovery path for the multitenancy beta (no email/SMTP). Part of dtask-y23 (Production deployment setup), epic dtask-tc7.\n\n## Implementation\n- scripts/reset_password.py \u003cemail\u003e: prompts interactively for a new password (never accept it as a bare CLI arg -- avoids shell-history/process-list leakage); reuses backend/app/auth.py's hash_password.\n- Connects directly to the DB via the DTASKS_DB_PATH env var (same one the app uses); looks up the user by lowercased/stripped email; updates password_hash.\n- Must work invoked against the Docker deployment, e.g. docker compose exec app python scripts/reset_password.py \u003cemail\u003e, since the DB lives on a volume mounted into the container. Invocation gets documented in docs/deploy.md.\n\n## Verification\n- Test: after running the script logic against a test DB, login with the old password fails and login with the new one succeeds.\n\n## Note\nA prior attempt at this script existed (orphaned .pyc bytecode files were found in scripts/__pycache__ and backend/tests/__pycache__) but the .py source was never committed -- rebuilding from scratch.","acceptance_criteria":"Running the script against a real user updates their password hash such that the old password no longer authenticates and the new one does; script prompts for the password interactively rather than accepting it as a CLI argument.","status":"closed","priority":1,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-13T20:40:03Z","created_by":"Evgenii Prusov","updated_at":"2026-07-13T20:53:05Z","started_at":"2026-07-13T20:42:26Z","closed_at":"2026-07-13T20:53:05Z","close_reason":"Implemented scripts/reset_password.py + tests, all passing","dependencies":[{"issue_id":"dtask-y23.1","depends_on_id":"dtask-y23","type":"parent-child","created_at":"2026-07-13T22:40:03Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-9st","title":"Add default task projects for Work and Personal","description":"Create predefined default projects for Personal and Work sections, auto-assign tasks to them if no project is specified, and prompt user if ambiguous.","status":"closed","priority":1,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-06T20:55:04Z","created_by":"Evgenii Prusov","updated_at":"2026-07-13T19:37:01Z","started_at":"2026-07-06T20:55:06Z","closed_at":"2026-07-13T19:37:01Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-y23","title":"Production deployment setup (VPS, HTTPS, ops scripts)","description":"Phase 4 of multitenancy epic dtask-tc7. Depends on dtask-3l0 + dtask-13p (done). Data migration/backfill was descoped: the owner will re-enter their data manually in a fresh account, so no create_owner backfill is needed. The old pre-multitenancy rows (user_id IS NULL) in the dev DB are invisible to all accounts and can simply be deleted.\n\n## Implementation\n1. scripts/reset_password.py \u003cemail\u003e: prompts new password, updates the hash (reuse auth.hash_password). This is the manual recovery path — beta has no email/SMTP.\n2. Prod config: DTASKS_INVITE_CODE set; Secure cookie flag on via DTASKS_SECURE_COOKIES=1; session FileStore dir (DTASKS_SESSION_DIR) on persistent disk; DTASKS_DB_PATH pointing at a persistent SQLite file. Caddy in front for automatic HTTPS, reverse-proxying to uvicorn on localhost. systemd unit (or docker-compose) for the app process.\n3. Fresh prod DB: just run `alembic upgrade head` (no stamp needed on a brand-new DB); or rely on create_all at startup — pick one and document it.\n4. Ops notes in docs/deploy.md (or README section): env vars, build steps (npm run build so the backend serves frontend/dist), backup strategy (cron sqlite .backup or litestream), how to hand out the invite code.\n\n## Verification\n1. reset_password test: after running the script logic, login with the old password fails, with the new one succeeds.\n2. Manual smoke checklist on the VPS: friend signs up over HTTPS with the invite code; Set-Cookie shows Secure + HttpOnly + SameSite=Lax; anonymous /api/* returns 401.","acceptance_criteria":"A friend can sign up with the invite code over HTTPS on the deployed instance; session cookie has Secure+HttpOnly flags in prod; password reset script works; deploy steps are documented.","status":"in_progress","priority":1,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-03T19:17:20Z","created_by":"Evgenii Prusov","updated_at":"2026-07-06T20:39:22Z","started_at":"2026-07-06T20:39:22Z","dependencies":[{"issue_id":"dtask-y23","depends_on_id":"dtask-3l0","type":"blocks","created_at":"2026-07-03T21:19:14Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-y23","depends_on_id":"dtask-13p","type":"blocks","created_at":"2026-07-03T21:19:15Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-y23","depends_on_id":"dtask-tc7","type":"parent-child","created_at":"2026-07-03T21:19:12Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-13p","title":"Frontend: landing page, signup/login, route guard, logout","description":"Phase 3 of multitenancy epic dtask-tc7. Depends on dtask-xwy (auth endpoints). Public landing page, auth flows, route guard, logout. No cookie plumbing needed — app is same-origin in dev (Vite proxy /api -\u003e :8000) and prod (dist served by backend).\n\n## Implementation\n1. api/types.ts: User {id, email}. api/client.ts: authMe(), signup({email, password, invite_code}), login({email, password}), logout().\n2. Auth state in api/hooks.ts: useCurrentUser() = useQuery({queryKey: ['auth','me'], queryFn: api.authMe, retry: false, staleTime: Infinity}). Mutations: useSignup / useLogin (on success invalidate ['auth','me'] and navigate '/'), useLogout (POST logout, qc.clear(), navigate '/welcome').\n3. Router restructure (router.tsx): rootRoute currently renders Layout directly. Make root pathless with a plain Outlet; add (a) /welcome public route and (b) a pathless authed layout route (component Layout) wrapping all existing child routes. Authed layout beforeLoad: await queryClient.ensureQueryData for auth/me; on 401 throw redirect({to: '/welcome'}). /welcome beforeLoad: if already authed throw redirect({to: '/'}).\n4. Global 401 handling: construct QueryClient with QueryCache/MutationCache onError — if ApiError.status === 401 and current route isn't /welcome, redirect to /welcome (covers session expiry mid-use). Extract as a testable handleAuthError(err) helper; do NOT handle 401 per-hook.\n5. views/WelcomeView.tsx: short app pitch + form toggling between signup (email, password, invite code) and login (email, password); submit via useSignup/useLogin; render ApiError detail (403 bad invite, 409 duplicate email, 401 bad credentials); disable submit while pending.\n6. Sidebar: show current user's email + Logout button (useCurrentUser + useLogout). Existing Sidebar.test.tsx will need the auth query mocked.\n\n## TDD plan — vitest + testing-library, fetch mocked via vi.spyOn(globalThis, 'fetch') per existing convention in api/hooks.test.tsx\n1. hooks.test: useSignup POSTs /api/auth/signup with {email, password, invite_code}; invalidates ['auth','me'] on success.\n2. hooks.test: useLogin same for /api/auth/login; on error surfaces ApiError and leaves cache untouched.\n3. hooks.test: useLogout POSTs /api/auth/logout and clears the query cache.\n4. WelcomeView.test: renders signup fields; submits entered values; shows server error detail on 403/409/401; toggles to the login form.\n5. handleAuthError unit test: 401 -\u003e redirect target /welcome; other errors ignored.\n6. Router guard integration test (createMemoryHistory + real route tree): me -\u003e 401 renders WelcomeView for '/'; me -\u003e 200 renders Layout; /welcome while authed redirects to '/'.\n7. Playwright e2e (repo already has Playwright): visit / -\u003e redirected to /welcome -\u003e sign up with invite code -\u003e starter data visible -\u003e logout -\u003e login again -\u003e data still there. This is the end-to-end acceptance gate for the whole epic UI.","acceptance_criteria":"Anonymous visitor landing on any route is redirected to /welcome; can sign up with invite code and lands in the app with starter data visible; can log out and log back in; expired/invalid session mid-use redirects to /welcome instead of showing broken views; frontend tests cover guard and signup/login forms.","status":"closed","priority":1,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-03T19:17:19Z","created_by":"Evgenii Prusov","updated_at":"2026-07-03T20:52:13Z","started_at":"2026-07-03T20:47:56Z","closed_at":"2026-07-03T20:52:13Z","close_reason":"Implemented and verified all tests passing in parallel worktrees","dependencies":[{"issue_id":"dtask-13p","depends_on_id":"dtask-tc7","type":"parent-child","created_at":"2026-07-03T21:19:12Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-13p","depends_on_id":"dtask-xwy","type":"blocks","created_at":"2026-07-03T21:19:13Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-3l0","title":"Backend tenancy: scope all queries per user + isolation tests","description":"Phase 2 of multitenancy epic dtask-tc7. Depends on dtask-xwy — the user_id columns and the authed/multi-client test fixtures land there. This phase is pure application logic: scope every query to the authenticated user. Invariant to enforce: no handler ever queries by bare primary key.\n\n## Implementation\n1. DI: provide user: User to handlers (dependency reading request.user set by SessionAuth); add it to every existing handler signature in main.py.\n2. Helpers gain a user_id parameter baked into the WHERE clause:\n   - _get_project(session, project_id, user_id): add Project.user_id == user_id.\n   - _get_task(session, task_id, user_id): select(Task).join(Project).where(Task.id == task_id, Project.user_id == user_id).\n   - set_habit_log's habit lookup: add Habit.user_id == user_id.\n   - _active_must_have_count(session, user_id, exclude_task_id): join(Project) + Project.user_id == user_id. Without this, other users' must-haves consume a user's daily limit of 2 (existing global-count bug).\n3. List/create scoping:\n   - list_projects / list_habits: .where(\u003cModel\u003e.user_id == user.id).\n   - create_project: stamp user_id=user.id AND compute next position only over the user's own projects (select(Project.position).where(Project.user_id == user.id)) — otherwise positions interleave across users.\n   - create_task: parent fetched via scoped _get_project, inherits safety.\n   - reorder_task: task fetched scoped; the follow-up project fetch passes user.id too.\n4. Cross-tenant semantics: scoped helpers fall through to NotFoundException -\u003e 404, byte-identical to a nonexistent id. Never 403 — resource existence must not leak.\n\n## TDD plan — write tests/test_isolation.py first (two clients via make_client fixture from phase 1; users A and B each get starter data at signup)\n1. test_lists_are_scoped — A and B each see exactly their own 4 starter projects / 4 habits; id sets are disjoint.\n2. test_cross_tenant_project_404 — B GET/PATCH/DELETE on A's project id -\u003e 404 with the same body as a nonexistent id (e.g. 999999).\n3. test_cross_tenant_task_404 — B PATCH/DELETE/reorder A's task; B POST /api/projects/{A's project}/tasks -\u003e all 404; verify A's data unchanged afterwards.\n4. test_cross_tenant_habit_404 — B PUT /api/habits/{A's habit}/log -\u003e 404.\n5. test_must_have_limit_is_per_user — starter data ships with 2 active must-haves per user, i.e. the limit starts maxed: A unsetting one of theirs then setting another succeeds even while B sits at the limit; A at the limit still gets 409 independent of B's state.\n6. test_project_positions_are_per_user — A and B each create one project; both get position 4, not 4 and 5 (regression for the cross-user position interleave).\nRegression gate: test_projects.py and test_auth.py stay green.","acceptance_criteria":"All existing endpoints operate only on the authenticated user's data; isolation test suite passes: B gets 404 on all of A's resource IDs across every endpoint (GET/PATCH/DELETE/reorder/log), B's lists contain only B's rows, must-have limit is per-user.","status":"closed","priority":1,"issue_type":"feature","owner":"prusove@gmail.com","created_at":"2026-07-03T19:17:19Z","created_by":"Evgenii Prusov","updated_at":"2026-07-03T20:52:13Z","closed_at":"2026-07-03T20:52:13Z","close_reason":"Implemented and verified all tests passing in parallel worktrees","dependencies":[{"issue_id":"dtask-3l0","depends_on_id":"dtask-tc7","type":"parent-child","created_at":"2026-07-03T21:19:11Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-3l0","depends_on_id":"dtask-xwy","type":"blocks","created_at":"2026-07-03T21:19:13Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-xwy","title":"Backend auth: User model, sessions, auth endpoints","description":"Phase 1 of multitenancy epic dtask-tc7. Blocks dtask-3l0 (tenancy scoping) and dtask-13p (frontend). ALL schema changes for multitenancy land here in one Alembic revision (users table + nullable user_id on projects/habits) so phase 2 is pure application logic.\n\n## Dependencies to add (backend/pyproject.toml)\n- argon2-cffi (Argon2id password hashing)\n- alembic (first migration story; schema currently comes from Base.metadata.create_all)\n\n## Implementation\n1. models.py: User(id, email String(255) unique — store lowercased/stripped, password_hash String(255), created_at). Add to Project and Habit: user_id FK users.id ondelete=CASCADE, index=True, nullable at DB level until phase 4 backfills prod (application always sets it).\n2. Alembic: init with async template (alembic init -t async), sqlalchemy.url from main.DB_PATH, render_as_batch=True in env.py (SQLite ALTER TABLE limitations). Two revisions: 0001 baseline = current pre-multitenancy schema (so the existing prod DB can be stamped in phase 4), 0002 = users table + user_id columns. Tests keep using Base.metadata.create_all (models stay source of truth); Alembic is for real DBs.\n3. New module app/auth.py:\n   - hash_password/verify_password wrapping argon2.PasswordHasher (VerifyMismatchError -\u003e False).\n   - Route handlers under /api/auth: signup, login, logout, me.\n   - SessionAuth[User, ServerSideSessionBackend] from litestar.security.session_auth: retrieve_user_handler loads User by session[\"user_id\"]; ServerSideSessionConfig (max_age ~30 days, httponly, samesite=lax, secure driven by env flag); FileStore for session data (path from env, default .sessions/ next to DB); exclude /api/auth/signup + /api/auth/login + all non-/api paths (static + SPA fallback stay public).\n4. Endpoint behavior:\n   - POST /api/auth/signup {email, password, invite_code}: invite checked against DTASKS_INVITE_CODE env var via secrets.compare_digest; email lowercased/stripped, must contain \"@\"; password min 8 chars; duplicate email -\u003e 409; success -\u003e create user, seed_starter_data(session, user.id), request.set_session({\"user_id\": user.id}), 201 {id, email}.\n   - POST /api/auth/login {email, password}: any failure (unknown email or bad password) -\u003e 401 with identical generic detail \"Invalid email or password\"; verify against a dummy hash when user not found to equalize timing. Success sets session.\n   - POST /api/auth/logout: request.clear_session() -\u003e 204.\n   - GET /api/auth/me: {id, email} from request.user.\n   - Rate-limit /api/auth/* (litestar RateLimitConfig, e.g. 10/min) at the auth router level.\n5. seed.py: seed_if_empty(session) becomes seed_starter_data(session, user_id) — drop the emptiness guard, stamp user_id on the 4 projects + 4 habits. Remove seeding from lifespan in main.py.\n6. schemas.py: msgspec Structs SignupPayload, LoginPayload, UserOut.\n7. conftest.py: set DTASKS_INVITE_CODE for tests via monkeypatch. The client fixture now signs up a default user (AsyncTestClient keeps the session cookie automatically), so all existing tests keep passing with per-user seeded data. Add a make_client(email) factory fixture for multi-user tests — each user needs its own AsyncTestClient because the cookie jar is per client.\n\n## TDD plan — write tests/test_auth.py first, in this order\n1. test_anonymous_api_request_gets_401 — GET /api/projects without session -\u003e 401 (red against current code).\n2. test_signup_creates_account_with_starter_data — signup -\u003e 201 {id,email}; GET /api/auth/me -\u003e 200; GET /api/projects -\u003e 4 starter projects; GET /api/habits -\u003e 4 habits.\n3. test_signup_rejects_wrong_invite_code — 403; subsequent /me -\u003e 401 (no session created).\n4. test_signup_rejects_duplicate_email_case_insensitive — \"Foo@x.com\" then \"foo@X.COM\" -\u003e 409.\n5. test_signup_rejects_short_password — 7 chars -\u003e 400.\n6. test_login_logout_roundtrip — logout -\u003e /me 401; login -\u003e /me 200.\n7. test_login_failures_are_indistinguishable — wrong password vs unknown email: same status and same body.\n8. test_spa_routes_stay_public — GET / (non-/api path) is not 401.\nRegression gate: existing test_projects.py stays green via the authed client fixture.","acceptance_criteria":"Signup with correct invite code creates a user and starts a session; wrong invite code rejected; login/logout work; GET /api/auth/me returns user when authenticated, 401 otherwise; all /api/* except /api/auth/* and static files return 401 without a session; new user has starter demo data; alembic upgrade head builds the schema from scratch.","status":"closed","priority":1,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-03T19:17:18Z","created_by":"Evgenii Prusov","updated_at":"2026-07-03T19:50:28Z","started_at":"2026-07-03T19:36:50Z","closed_at":"2026-07-03T19:50:28Z","close_reason":"Implemented on multitenancy branch, commit d76e2bc: auth endpoints, sessions, alembic, 13 tests green, real-server smoke test passed","dependencies":[{"issue_id":"dtask-xwy","depends_on_id":"dtask-tc7","type":"parent-child","created_at":"2026-07-03T21:19:10Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dtask-tc7","title":"Multitenancy: per-user auth and data isolation","description":"Turn dtasks into a multi-user service for friends as beta-testers. Email/password accounts gated by an invite code (no OAuth), cookie sessions via Litestar SessionAuth, per-user data isolation enforced in every query (cross-tenant access returns 404), landing page with signup/login, starter demo data per new user, existing DB data migrated to the owner's account. Stack stays Litestar + SQLAlchemy + SQLite on a single VPS. Design agreed in chat 2026-07-03.","acceptance_criteria":"Two users can sign up and use the app concurrently; neither can read or modify the other's projects/tasks/habits by any API call; unauthenticated requests to /api/* (except /api/auth/*) get 401; the app is deployed behind HTTPS with an invite code. (Data migration descoped: owner re-enters data manually.)","status":"open","priority":1,"issue_type":"epic","owner":"prusove@gmail.com","created_at":"2026-07-03T19:16:52Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T06:31:38Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-5eo","title":"Harden CI/CD: dedupe CI triggers + require status checks on main","description":"Found during review of PR #56's merge. Two gaps: (1) ci.yml triggers on both 'push' and 'pull_request' with no branch scoping, causing every commit on an open PR branch to fire CI twice. (2) main's branch protection has no required_status_checks configured at all -- CI results are not enforced as a merge gate, and cd.yml deploys independently on push to main with no dependency on CI passing.","acceptance_criteria":"ci.yml push trigger scoped to avoid duplicate runs with pull_request on the same commit; main branch protection requires the three CI job status checks (Backend, Frontend, Terraform) before merge.","status":"closed","priority":2,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-18T12:42:05Z","created_by":"Evgenii Prusov","updated_at":"2026-07-18T12:43:02Z","started_at":"2026-07-18T12:42:08Z","closed_at":"2026-07-18T12:43:02Z","close_reason":"Fixed in PR #57: ci.yml push trigger scoped to main (stops duplicate CI runs on PR branches); main branch protection now requires the 3 CI job status checks before merge.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-5bv.5","title":"E2E verification with real Google/GitHub apps + prod rollout","description":"Part of epic dtask-5bv (OAuth social login). Final gate before calling the epic done. Depends on all other epic tasks being merged. NEEDS A HUMAN for the credential steps — an agent can prepare and verify everything around them but cannot create OAuth apps or paste secrets (flag with bd human if blocked).\n\nContext: dtasks runs as Docker + Caddy on a single VM (docs/deploy.md has the full runbook, including how to deploy an updated image). OAuth design: docs/auth.md §2. Local dev: backend cd backend \u0026\u0026 uv sync \u0026\u0026 uv run litestar --app app.main:app run --port 8000; frontend cd frontend \u0026\u0026 npm install \u0026\u0026 npm run dev (serves :5173, proxies /api).\n\nChecklist:\n1. Human: register dev OAuth apps for Google and GitHub with callback http://localhost:5173/api/auth/oauth/\u003cprovider\u003e/callback (steps in docs/deploy.md after the docs task) and provide dev client IDs/secrets via env.\n2. Dev-run the full matrix for BOTH providers: fresh signup with valid invite (user created, starter demo data seeded, lands on Today view); signup without invite -\u003e invite_required banner on /welcome; wrong invite -\u003e invalid_invite; returning OAuth login; auto-link (create a password account first, then OAuth-login with the same email — must reuse the account, and an oauth_accounts row must appear); OAuth-only account password login -\u003e generic 401; cancel/deny on the provider consent screen -\u003e provider_error banner.\n3. Confirm alembic upgrade head runs cleanly against a COPY of the production SQLite DB (backend/db_backups/ has snapshots; docs/deploy.md covers how migrations run via the Docker entrypoint).\n4. Human: register prod OAuth apps with callback https://\u003cDOMAIN\u003e/api/auth/oauth/\u003cprovider\u003e/callback, add the four secrets to /srv (or wherever .env lives per docs/deploy.md) on the VM.\n5. Deploy per docs/deploy.md; on prod verify: both buttons render, one full Google and one full GitHub login round-trip, session cookie has Secure flag, no provider tokens in docker compose logs.\n6. Record any deviations found as new bd issues; update docs/auth.md if behavior differs.\n\nWorkflow: any code/doc fixes go through feature branch + PR (main is hook-protected); bd update \u003cthis-id\u003e --claim to start, bd close when the prod checklist is green.","acceptance_criteria":"Full §2.5 decision-tree matrix verified against real Google and GitHub in dev; prod deployment serves working Google + GitHub login over HTTPS; migration verified against a prod DB copy before deploy; no tokens in logs; deviations filed as bd issues.","status":"open","priority":2,"issue_type":"task","owner":"prusove@gmail.com","created_at":"2026-07-17T21:42:10Z","created_by":"Evgenii Prusov","updated_at":"2026-07-17T23:42:19Z","dependencies":[{"issue_id":"dtask-5bv.5","depends_on_id":"dtask-5bv.2","type":"blocks","created_at":"2026-07-17T23:42:19Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5bv.5","depends_on_id":"dtask-5bv","type":"parent-child","created_at":"2026-07-17T23:42:10Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5bv.5","depends_on_id":"dtask-5bv.4","type":"blocks","created_at":"2026-07-17T23:42:20Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5bv.5","depends_on_id":"dtask-5bv.3","type":"blocks","created_at":"2026-07-17T23:42:20Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-5bv.4","title":"Docs \u0026 deploy: OAuth env vars, provider app registration, compose wiring","description":"Part of epic dtask-5bv (OAuth social login). Spec: docs/auth.md §2.8 (configuration) and §2.4 (providers). Depends on the backend OAuth endpoints task (env var names must match the merged code). This is a docs/config task — no application code.\n\nWork:\n1. .env.example: add commented DTASKS_GOOGLE_CLIENT_ID, DTASKS_GOOGLE_CLIENT_SECRET, DTASKS_GITHUB_CLIENT_ID, DTASKS_GITHUB_CLIENT_SECRET with a note that leaving a pair unset disables that provider's button.\n2. docker-compose.yml: in the app service environment block set DTASKS_PUBLIC_URL: https://${DOMAIN} (DOMAIN already comes from .env for Caddy). The four secrets need no compose changes — env_file: .env already injects everything in .env.\n3. docs/deploy.md: extend the env-var table with the five new vars (source + purpose, matching the existing table style). Add a short \"OAuth providers\" section: create SEPARATE dev and prod OAuth apps (a GitHub OAuth app allows exactly one callback URL); prod callback URLs are https://\u003cDOMAIN\u003e/api/auth/oauth/google/callback and .../github/callback; dev callback URLs use http://localhost:5173 (Vite proxies /api to the backend). Google: Cloud Console -\u003e APIs \u0026 Services -\u003e Credentials -\u003e OAuth client ID (Web application), consent screen with the openid/email/profile scopes, add the callback as an authorized redirect URI. GitHub: Settings -\u003e Developer settings -\u003e OAuth Apps -\u003e New OAuth App. Step-by-step, written for someone doing it the first time.\n4. README.md: add the auth endpoints (signup/login/logout/me + the three OAuth routes from docs/auth.md §2.6) to the API table, and a one-line pointer to docs/auth.md.\n5. Re-read docs/auth.md against the merged backend implementation and fix any drift (env var names, error codes, endpoint paths) so the doc matches shipped behavior.\n\nVerify: docker compose config (with a dummy .env) renders DTASKS_PUBLIC_URL correctly; markdown renders cleanly.\n\nWorkflow: feature branch + PR (main is hook-protected); bd update \u003cthis-id\u003e --claim to start, bd close when merged-ready.","acceptance_criteria":"A first-time operator can register both OAuth apps and fill .env using only docs/deploy.md; compose injects DTASKS_PUBLIC_URL=https://\u003cDOMAIN\u003e; README API table lists auth routes; docs/auth.md matches the shipped code.","status":"open","priority":2,"issue_type":"task","owner":"prusove@gmail.com","created_at":"2026-07-17T21:41:48Z","created_by":"Evgenii Prusov","updated_at":"2026-07-17T23:42:18Z","dependencies":[{"issue_id":"dtask-5bv.4","depends_on_id":"dtask-5bv","type":"parent-child","created_at":"2026-07-17T23:41:48Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5bv.4","depends_on_id":"dtask-5bv.2","type":"blocks","created_at":"2026-07-17T23:42:18Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-5bv.3","title":"Frontend: social login buttons + OAuth error handling on /welcome","description":"Part of epic dtask-5bv (OAuth social login). Spec: docs/auth.md §2.9 (frontend), §2.7 (error codes), §2.6 (the /api/auth/providers endpoint this consumes). Depends on the backend OAuth endpoints task. Stack: React 19 + TypeScript, TanStack Router + Query, Tailwind CSS 4, vitest + testing-library; i18n via react-i18next with frontend/src/i18n/en.json and ru.json (keep both in sync — the resources.d.ts types are generated from en.json).\n\nFiles to touch:\n1. frontend/src/api/hooks.ts (+ types.ts, client.ts if needed): add useAuthProviders — a query for GET /api/auth/providers returning {google: boolean, github: boolean}. It must work unauthenticated (the welcome page is pre-login).\n2. frontend/src/views/WelcomeView.tsx: below the submit button add an \"or\" divider and \"Continue with Google\" / \"Continue with GitHub\" buttons (inline SVG provider marks; follow the existing Tailwind utility style — .card, .btn classes, text-ink-* tokens). Render each button only if its provider is enabled. Login mode: window.location.href = \"/api/auth/oauth/\u003cprovider\u003e/login\". Signup mode: require non-empty invite code first — if empty, show a localized inline error (welcome.inviteRequiredForOauth) and do not navigate; else append ?invite_code=\u003cencodeURIComponent(code)\u003e.\n3. OAuth error banner: the backend redirects failures to /welcome?oauth_error=\u003ccode\u003e with codes invite_required | invalid_invite | no_verified_email | state_mismatch | provider_error. Read the param via TanStack Router search params on the /welcome route (frontend/src/router.tsx welcomeRoute — add validateSearch), display the localized message in the existing error slot in WelcomeView, and strip the param from the URL after showing it. Unknown codes fall back to common.genericError.\n4. i18n: add to BOTH en.json and ru.json: welcome.continueWithGoogle, welcome.continueWithGithub, welcome.or, welcome.inviteRequiredForOauth, welcome.errors.oauth.\u003ceach code above\u003e. Match the tone of existing welcome.* strings; natural Russian, not machine-literal.\n\nTests (frontend/src/views/WelcomeView.test.tsx exists — extend it; mock fetch/query data the way existing tests do): buttons render per providers response and hide when disabled; signup-mode click with empty invite shows the inline error and does not navigate; login-mode click navigates to the right URL; oauth_error=invalid_invite renders the localized message; unknown code falls back to generic.\nRun: cd frontend \u0026\u0026 npm test (vitest + playwright; if playwright browsers are missing in the sandbox, run npx vitest run and note that in the handoff).\n\nWorkflow: feature branch + PR (main is hook-protected); bd update \u003cthis-id\u003e --claim to start, bd close when merged-ready.","acceptance_criteria":"Buttons appear per /api/auth/providers; signup without invite blocks navigation with a localized inline error; every §2.7 oauth_error code renders a translated banner in en and ru; vitest suite passes; oxlint (npm run lint) clean.","status":"open","priority":2,"issue_type":"task","owner":"prusove@gmail.com","created_at":"2026-07-17T21:41:47Z","created_by":"Evgenii Prusov","updated_at":"2026-07-17T23:42:18Z","dependencies":[{"issue_id":"dtask-5bv.3","depends_on_id":"dtask-5bv","type":"parent-child","created_at":"2026-07-17T23:41:46Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5bv.3","depends_on_id":"dtask-5bv.2","type":"blocks","created_at":"2026-07-17T23:42:18Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-5bv.2","title":"Backend: OAuth login/callback endpoints for Google \u0026 GitHub","description":"Part of epic dtask-5bv (OAuth social login). Read docs/auth.md before coding — §2.2 (flow), §2.3 (library decision: plain httpx, no OAuth lib, no id_token validation), §2.4 (exact provider URLs/params), §2.5 (callback decision tree), §2.6 (endpoint table), §2.7 (error codes), §2.8 (env vars), §2.10 (security), §2.11 (test list). That file is the contract; do not invent alternative flows. Depends on the schema task (oauth_accounts + nullable password_hash) being merged.\n\nRepo context: backend/app/auth.py holds all auth (argon2 password auth, Litestar SessionAuth with server-side FileStore sessions, auth_router mounted at /api/auth with a rate-limited subrouter for signup/login). Signup seeds demo data via seed_starter_data(session, user.id) from backend/app/seed.py — OAuth signups must do the same. DB access: async SQLAlchemy session via the \"session\" dependency (see existing handlers).\n\nWork (new module backend/app/oauth.py recommended, wired into auth_router):\n1. GET /api/auth/oauth/{provider:str}/login — exclude_from_auth, inside the existing rate-limited subrouter. Optional invite_code query param. 404 for unknown provider or missing env creds. Generates state (secrets.token_urlsafe(32)) and, for Google, a PKCE S256 verifier/challenge; stores {provider, state, code_verifier, invite_code, created_at} in the server-side session under an \"oauth\" key (works pre-auth; SameSite=Lax is fine because these are top-level GET navigations); 302 to the provider authorize URL built per §2.4 with redirect_uri = DTASKS_PUBLIC_URL + /api/auth/oauth/{provider}/callback (DTASKS_PUBLIC_URL defaults to http://localhost:5173 — dev goes through the Vite proxy).\n2. GET /api/auth/oauth/{provider:str}/callback — exclude_from_auth, rate-limited. Pop and validate stored state (single-use, max age 10 min) else redirect /welcome?oauth_error=state_mismatch. Exchange code and fetch profile with httpx exactly per §2.4 (GitHub: Accept: application/json on token POST; email from /user/emails picking primary \u0026\u0026 verified; store numeric id as string). Then the §2.5 decision tree: existing (provider, provider_account_id) → login; verified-email match on users → auto-link + login; else signup requiring valid invite code from stored state (compare with secrets.compare_digest against DTASKS_INVITE_CODE) → create user with password_hash=None, insert oauth_accounts row, seed_starter_data, login. Success: request.set_session({\"user_id\": ...}) then 302 to /. Failures redirect to /welcome?oauth_error=\u003ccode\u003e using only the codes in §2.7. Never persist provider tokens; never log them.\n3. GET /api/auth/providers — exclude_from_auth, returns {\"google\": bool, \"github\": bool} from env-var presence.\n4. pyproject.toml: move httpx from dev to runtime dependencies; add respx to dev-dependencies; run uv sync (uv.lock changes).\n\nTests: new backend/tests/test_oauth.py mocking provider HTTP with respx; implement the full checklist in docs/auth.md §2.11 (redirect params, state lifecycle incl. replay + expiry, all decision-tree branches, invite gating, GitHub verified-email selection, unconfigured provider 404). Follow conftest.py patterns (env vars read at import time — set DTASKS_GOOGLE_CLIENT_ID etc. before importing the app in tests, mirroring how DTASKS_INVITE_CODE is handled).\nRun: cd backend \u0026\u0026 uv run pytest\n\nWorkflow: feature branch + PR (main is hook-protected); bd update \u003cthis-id\u003e --claim to start, bd close when merged-ready.","acceptance_criteria":"All three endpoints behave per docs/auth.md §2.4–§2.8; every §2.11 backend test case exists and passes; provider tokens exist only as local variables; unknown/unconfigured providers 404; full backend suite passes with uv run pytest.","status":"open","priority":2,"issue_type":"task","owner":"prusove@gmail.com","created_at":"2026-07-17T21:41:14Z","created_by":"Evgenii Prusov","updated_at":"2026-07-18T14:27:34Z","dependencies":[{"issue_id":"dtask-5bv.2","depends_on_id":"dtask-5bv","type":"parent-child","created_at":"2026-07-17T23:41:13Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5bv.2","depends_on_id":"dtask-5bv.1","type":"blocks","created_at":"2026-07-17T23:42:17Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"dtask-5bv.1","title":"Backend: nullable password_hash + oauth_accounts table (migration 0004)","description":"Part of epic dtask-5bv (OAuth social login). Read docs/auth.md first — §2.5 (identity model) and §2.6 (login change) are the spec for this task. Repo layout: Litestar 2 + SQLAlchemy 2 async + SQLite backend in backend/, Alembic migrations in backend/migrations/versions/ (latest is 0003_green_tasks.py; follow the style of 0002_multitenancy.py, which uses batch_alter_table for SQLite).\n\nWork:\n1. backend/app/models.py — make User.password_hash nullable (Mapped[str | None]); add OAuthAccount model per the DDL in docs/auth.md §2.5: id PK, user_id FK -\u003e users.id ON DELETE CASCADE (indexed, NOT NULL), provider (String(20)), provider_account_id (String(255)), email (String(255)), created_at (timezone-aware UTC default, same pattern as User.created_at), UNIQUE(provider, provider_account_id).\n2. New migration backend/migrations/versions/0004_oauth_accounts.py (revision \"0004\", down_revision \"0003\"): create oauth_accounts, alter users.password_hash to nullable via batch_alter_table. Provide a real downgrade.\n3. backend/app/auth.py login handler: a user whose password_hash is NULL must behave exactly like an unknown email — verify the submitted password against the module-level _DUMMY_HASH and return the same generic 401 (\"Invalid email or password\"). No response difference (status, body, timing path) between unknown email, wrong password, and OAuth-only account.\n4. Confirm scripts/reset_password.py still works against a NULL-hash user (it sets a new hash — that is the intended manual way to give an OAuth-only account a password). Adjust only if something breaks.\n\nTests (backend/tests/, follow conftest.py — env vars are read at import time, tests use a throwaway SQLite file and DTASKS_AUTO_CREATE_SCHEMA=1):\n- test_auth.py: password login for a user with password_hash=None returns 401 with the same body as a wrong password.\n- test_reset_password.py: resetting a NULL-hash user sets a working password.\nRun: cd backend \u0026\u0026 uv run pytest\n\nWorkflow: feature branch + PR (direct commits to main are blocked by hooks); claim with bd update \u003cthis-id\u003e --claim, close with bd close when merged-ready.","acceptance_criteria":"alembic upgrade head applies 0004 on a copy of an existing DB; downgrade reverts it; OAuthAccount model matches docs/auth.md §2.5; OAuth-only users get the generic 401 on password login; full backend test suite passes.","status":"closed","priority":2,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-17T21:41:13Z","created_by":"Evgenii Prusov","updated_at":"2026-07-18T12:27:35Z","started_at":"2026-07-18T12:23:06Z","closed_at":"2026-07-18T12:27:35Z","close_reason":"Implemented in PR https://github.com/evgenii-prusov/dtasks/pull/56 (branch oauth-schema-foundation-dtask-5bv-1). Full test suite passes (32/32); migration verified end-to-end (upgrade head / downgrade -1) on a seeded SQLite DB.","dependencies":[{"issue_id":"dtask-5bv.1","depends_on_id":"dtask-5bv","type":"parent-child","created_at":"2026-07-17T23:41:12Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-5bv","title":"OAuth social login: Google \u0026 GitHub alongside password auth","description":"Add \"Continue with Google\" and \"Continue with GitHub\" to dtasks (Litestar + SQLAlchemy/SQLite backend, React 19 SPA frontend) on top of the existing invite-gated email/password auth from epic dtask-tc7. The full agreed design is in docs/auth.md §2 in this repo — it is the contract for all child tasks: server-side Authorization Code flow handled entirely by the backend, same cookie-session model as today, provider tokens never stored, new oauth_accounts table, users.password_hash becomes nullable, OAuth signups still require the invite code, accounts auto-link by provider-verified email.\n\nChild tasks are self-sufficient: each names its files, references docs/auth.md sections, and carries its own acceptance criteria. Execution order: schema task first, then backend endpoints, then frontend + docs/deploy in parallel, then E2E rollout (needs human-provided OAuth app credentials).","design":"docs/auth.md §2 (flow §2.2, providers §2.4, account rules §2.5, endpoints §2.6, config §2.8, frontend §2.9, tests §2.11)","acceptance_criteria":"A user can sign up (with invite code) and log in via Google or GitHub end-to-end on the deployed HTTPS instance; a password user with the same verified email gets auto-linked, not duplicated; OAuth-only accounts get a generic 401 on password login; provider tokens are never persisted; all backend + frontend tests pass; docs/auth.md and docs/deploy.md describe the shipped behavior.","status":"open","priority":2,"issue_type":"epic","owner":"prusove@gmail.com","created_at":"2026-07-17T21:40:32Z","created_by":"Evgenii Prusov","updated_at":"2026-07-17T21:42:34Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-7q7","title":"Allow changing order of projects in Personal and Work sections","description":"Enable up/down controls on sidebar project list items and support reordering on the backend.","status":"closed","priority":2,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-06T20:18:22Z","created_by":"Evgenii Prusov","updated_at":"2026-07-06T20:18:26Z","started_at":"2026-07-06T20:18:24Z","closed_at":"2026-07-06T20:18:26Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-bc4","title":"Allow assigning tasks to Today or Week from Review view","description":"Add Today and Week toggle buttons to open tasks in the Review view, matching the styling and behavior of the Plan view.","status":"in_progress","priority":2,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-06T20:08:37Z","created_by":"Evgenii Prusov","updated_at":"2026-07-06T20:08:38Z","started_at":"2026-07-06T20:08:38Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-v6h","title":"Allow renaming existing projects from the UI","description":"Backend PATCH /api/projects/{id} already accepts 'name' (ProjectPatch), but the frontend renders the project title as static text in ProjectView with no edit affordance. Users who create a project with the wrong name must delete it and re-create all tasks. Add click-to-rename on the project title in ProjectView, following the TaskRow click-to-edit idiom (Enter=save, Escape=cancel, blur=save, empty input falls back to old name). Add i18n keys in en+ru.","status":"closed","priority":2,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-05T07:24:42Z","created_by":"Evgenii Prusov","updated_at":"2026-07-05T07:35:36Z","started_at":"2026-07-05T07:24:48Z","closed_at":"2026-07-05T07:35:36Z","close_reason":"Implemented click-to-rename on project title in ProjectView; verified end-to-end with Playwright against the running app","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-z5x","title":"i18n phase 4: Cyrillic font subset + Russian visual pass","description":"Plus Jakarta Sans ships no base Cyrillic (U+0400-045F), only cyrillic-ext; Russian falls back to system sans. Load a Cyrillic-only companion subset (e.g. @fontsource/inter cyrillic) after PJS in --font-sans so Latin stays pixel-identical. Then click through every view in Russian: 230px sidebar, btn-s buttons, tracked uppercase micro-labels (START HERE), text ~20-30% longer. Part of dtask-5lq.","status":"closed","priority":2,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-04T20:26:13Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T22:39:16Z","started_at":"2026-07-04T22:32:57Z","closed_at":"2026-07-04T22:39:16Z","close_reason":"Inter cyrillic subset backs Plus Jakarta Sans for Russian glyphs (unicode-ranged, on-demand download verified in-browser); full ru visual pass over all 7 views incl. dark mode via screenshots - no overflow or fallback-font issues","dependencies":[{"issue_id":"dtask-z5x","depends_on_id":"dtask-068","type":"blocks","created_at":"2026-07-04T22:26:23Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dtask-068","title":"i18n phase 3: Russian translations, plurals, localized dates","description":"Translate ru.json (key set must mirror en.json - parity test enforces). Use i18next _one/_few/_many plural forms for counts. Replace hardcoded en-US locale in TodayView and HabitsView with a formatDate() helper in lib/dates.ts that uses the current language and uppercases the first letter of ru weekday/month names in headings. Part of dtask-5lq.","status":"closed","priority":2,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-04T20:26:12Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T22:29:10Z","started_at":"2026-07-04T22:24:10Z","closed_at":"2026-07-04T22:29:10Z","close_reason":"ru.json fully translated (Must Have -\u003e «Главное»), ru plurals for the must-limit alert, dates/weekday/month labels localized via Intl with capitalization helper; 51/51 vitest + real-browser ru check passed","dependencies":[{"issue_id":"dtask-068","depends_on_id":"dtask-ya0","type":"blocks","created_at":"2026-07-04T22:26:22Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dtask-ya0","title":"i18n phase 2: extract all UI strings to the en catalog","description":"Move ~110 hardcoded strings into en.json view by view: sidebar \u003e today \u003e plan \u003e review \u003e habits \u003e project \u003e welcome \u003e shared components, including confirm()/alert() messages and ApiError fallbacks (map 401 login to a catalog key; generic localized fallback elsewhere). English only; zero visible behavior change. Part of dtask-5lq.","status":"closed","priority":2,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-04T20:26:12Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T22:20:07Z","started_at":"2026-07-04T22:07:54Z","closed_at":"2026-07-04T22:20:07Z","close_reason":"All ~110 UI strings extracted to en.json (ru.json mirrors keys, translation is dtask-068); build/lint/vitest/e2e green with zero visible change","dependencies":[{"issue_id":"dtask-ya0","depends_on_id":"dtask-srp","type":"blocks","created_at":"2026-07-04T22:26:22Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dtask-srp","title":"i18n phase 1: foundation — i18next setup, typed catalogs, language toggle","description":"Install i18next + react-i18next. Add src/i18n/ (index.ts init, en.json, ru.json, typed resources via module augmentation). Add useLanguage() hook mirroring theme.ts (localStorage, document.documentElement.lang, changeLanguage) and an EN/RU toggle in the Sidebar footer. Init i18n with lng=en in src/test/setup.ts; add en/ru catalog key-parity test. Migrate the 4 sidebar nav labels as an end-to-end smoke test of the pipeline. Part of dtask-5lq.","status":"closed","priority":2,"issue_type":"task","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-04T20:26:11Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T22:05:08Z","started_at":"2026-07-04T20:26:27Z","closed_at":"2026-07-04T22:05:08Z","close_reason":"Implemented and verified: i18next foundation, typed catalogs, useLanguage + sidebar toggle, test setup; vitest 46/46, e2e signup flow + real-browser toggle check passed","dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dtask-5lq","title":"i18n: support English and Russian UI languages","description":"The app UI is English-only with ~110 hardcoded strings across 7 views and 5 components, plus en-US-hardcoded date formatting and browser confirm()/alert() messages. Add an i18n layer so the UI can render fully in English or Russian, with a language switcher, persisted preference, and correct Russian plural/date handling.","design":"Library: react-i18next with statically bundled JSON catalogs (src/i18n/en.json, ru.json), typed keys via module augmentation. Language state: useLanguage() hook mirroring theme.ts (localStorage + document.documentElement.lang), EN/RU toggle in Sidebar footer. Dates: replace hardcoded 'en-US' locale in TodayView/HabitsView with current language; capitalize ru weekday in headings. Plurals: i18next _one/_few/_many/_other suffixes (Intl.PluralRules handles Russian). Backend stays English; frontend maps ApiError by status/known-case to localized messages (error codes on the wire as optional later step). FONT GOTCHA: Plus Jakarta Sans ships NO base Cyrillic subset (U+0400-045F) - only cyrillic-ext (historic letters); Russian would fall back to system sans. Fix: load a Cyrillic-only companion subset (e.g. Inter cyrillic) after PJS in --font-sans so Latin rendering stays identical. Lora (serif) already has Cyrillic. Tests: init i18n with lng=en in src/test/setup.ts so existing English assertions keep passing; add en/ru catalog key-parity test. Out of scope: seeded demo data, DTask brand name, backend content.","status":"closed","priority":2,"issue_type":"epic","owner":"prusove@gmail.com","created_at":"2026-07-04T20:19:41Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T22:39:17Z","closed_at":"2026-07-04T22:39:17Z","close_reason":"All four phases implemented and verified; PR stack #23-#26 pending review/merge","dependencies":[{"issue_id":"dtask-5lq","depends_on_id":"dtask-srp","type":"blocks","created_at":"2026-07-04T22:26:24Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5lq","depends_on_id":"dtask-ya0","type":"blocks","created_at":"2026-07-04T22:26:24Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5lq","depends_on_id":"dtask-z5x","type":"blocks","created_at":"2026-07-04T22:26:25Z","created_by":"Evgenii Prusov","metadata":"{}"},{"issue_id":"dtask-5lq","depends_on_id":"dtask-068","type":"blocks","created_at":"2026-07-04T22:26:25Z","created_by":"Evgenii Prusov","metadata":"{}"}],"dependency_count":4,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-n6e","title":"Add habit creation (backend endpoint + UI form)","description":"Currently there is no way to add a new habit — the app only ships list_habits, set_habit_log, and delete_habit (backend/app/main.py:254-301); habits only exist via the per-user starter seed at signup (backend/app/seed.py). Once a user deletes a starter habit, there's no way to replace it, and users can't track any new habit beyond the 4 seeded ones.\n\n## Implementation\n1. Backend: POST /api/habits (name required, subtitle optional, default position = max+1 over the user's own habits, mirroring create_project's pattern at main.py:123-140). Add HabitCreate to schemas.py. Register the handler + reuse existing user-scoping conventions (Habit.user_id == user.id).\n2. Frontend: api/client.ts createHabit(); api/hooks.ts useCreateHabit() (invalidate ['habits'] on success, mirroring useCreateProject); a small \"New Habit\" form/button in HabitsView.tsx, similar to AddTaskForm's pattern.\n\n## Tests (TDD)\n- Backend: test creating a habit returns 201 with the new habit (empty log); rejects blank name; new habit is scoped to the creating user (isolation test consistent with tests/test_isolation.py conventions).\n- Frontend: hook test for useCreateHabit (POST + cache invalidation), component test for the new habit form in HabitsView.","acceptance_criteria":"POST /api/habits creates a habit for the authenticated user only and returns it; blank name rejected; UI has a visible way to add a new habit from the Habits view; existing habit tests (list/log/delete/isolation) remain green.","status":"closed","priority":2,"issue_type":"feature","owner":"prusove@gmail.com","created_at":"2026-07-04T10:00:31Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T15:28:03Z","closed_at":"2026-07-04T15:28:03Z","close_reason":"Implemented POST /api/habits (backend, TDD) plus frontend createHabit/useCreateHabit and an AddHabitForm wired into HabitsView behind an Add toggle. All backend (pytest) and frontend (vitest) suites pass; verified end-to-end in a headless browser.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dtask-flc","title":"Delete habits (backend endpoint + frontend UI)","description":"Habits currently support list (GET /api/habits) and daily logging (PUT /api/habits/{id}/log), but there is no way to remove a habit once created. Add delete support end-to-end, following TDD (write the failing test first, then the minimal code to pass it).\n\n## Backend (write tests first in backend/tests/test_habits.py, a new file)\n1. test_delete_habit_removes_it_and_cascades_logs — mirrors test_delete_project_removes_it_and_cascades_tasks in test_projects.py. Seed/log a habit, DELETE /api/habits/{id}, assert 204, assert it's gone from GET /api/habits, and assert no orphan HabitLog rows remain (habit_logs.habit_id has ondelete=CASCADE already).\n2. test_delete_missing_habit_returns_404 — DELETE /api/habits/999999 -\u003e 404.\n3. In test_isolation.py: test_cross_tenant_habit_delete_404 — user B cannot delete user A's habit (mirrors the existing test_cross_tenant_habit_404 pattern for the log endpoint) -\u003e 404, and the habit still exists for user A afterward.\n\nThen implement just enough to pass:\n- Add a `_get_habit(session, habit_id, user_id) -\u003e Habit` helper in backend/app/main.py, following the existing `_get_task`/`_get_project` pattern (raise NotFoundException if missing or not owned by user).\n- Add `delete_habit` handler: `@delete(\"/api/habits/{habit_id:int}\", status_code=204)`, delete + commit, same shape as delete_project/delete_task.\n- Register `delete_habit` in `route_handlers`.\n\n## Frontend (write test first)\n1. In frontend/src/api/hooks.test.tsx, add a `describe('useDeleteHabit')` block mirroring the existing `useDeleteProject` tests (calls DELETE, invalidates/updates the ['habits'] query cache).\n2. Add `deleteHabit: (id: number) =\u003e request\u003cvoid\u003e(\\`/api/habits/${id}\\`, { method: 'DELETE' })` to frontend/src/api/client.ts.\n3. Add `useDeleteHabit()` to frontend/src/api/hooks.ts, mirroring `useDeleteTask`/`useDeleteProject`.\n4. Wire a delete action into frontend/src/views/HabitsView.tsx's per-habit card (icon/button), using the same `confirm(...)` pattern as TaskRow.tsx before calling the mutation — no new modal component needed.\n\n## Design decisions\n- Reuse existing `_get_task`/`_get_project` helper pattern for `_get_habit` rather than inventing a new lookup style.\n- Reuse the plain `window.confirm()` pattern from TaskRow.tsx for delete confirmation instead of building a custom dialog.\n- Rely on the existing DB-level CASCADE on habit_logs.habit_id; no manual cleanup of logs needed in the handler.","acceptance_criteria":"- backend/tests/test_habits.py exists with test_delete_habit_removes_it_and_cascades_logs and test_delete_missing_habit_returns_404, both passing\n- backend/tests/test_isolation.py has test_cross_tenant_habit_delete_404, passing\n- DELETE /api/habits/{habit_id} implemented in backend/app/main.py, returns 204 on success, 404 when habit missing or owned by another user\n- frontend/src/api/hooks.test.tsx has passing tests for useDeleteHabit\n- frontend/src/api/client.ts and hooks.ts expose deleteHabit/useDeleteHabit\n- HabitsView.tsx has a working delete action (with confirm) per habit, manually verified in the browser\n- All backend (pytest) and frontend (vitest) test suites pass","status":"closed","priority":2,"issue_type":"feature","assignee":"Evgenii Prusov","owner":"prusove@gmail.com","created_at":"2026-07-04T09:35:42Z","created_by":"Evgenii Prusov","updated_at":"2026-07-04T09:46:40Z","started_at":"2026-07-04T09:35:58Z","closed_at":"2026-07-04T09:46:40Z","close_reason":"Implemented DELETE /api/habits/{id} + frontend delete action, TDD (backend and frontend tests written first and confirmed failing before implementation). All suites green.","dependency_count":0,"dependent_count":0,"comment_count":0}
